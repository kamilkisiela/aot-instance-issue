{
  "name": "example-package",
  "version": "0.0.0",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "build": "./node_modules/.bin/ngc -p tsconfig.json",
    "prebuild": "rimraf build/ && rimraf compiled/",
    "prepublish": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@angular/compiler": "2.3.1",
    "@angular/compiler-cli": "2.3.1",
    "@angular/core": "2.3.1",
    "rxjs": "5.0.0-rc.4"
  },
  "devDependencies": {
    "rimraf": "^2.5.4",
    "typescript": "~2.0.10"
  }
}
